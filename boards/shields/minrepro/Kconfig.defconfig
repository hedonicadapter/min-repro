if MINREPRO_LEFT

# Name must be less than 16 characters long!
config ZMK_KEYBOARD_NAME
    default "Minrepro"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if MINREPRO_LEFT || MINREPRO_RIGHT

config ZMK_SPLIT
    default y

endif
